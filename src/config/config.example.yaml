database:
  server: "localhost" # use ip and port connection (standard tcp/ip SQL Connection)
  database: "your_database_name" 
  username: "your_sql_user" # create a sql login 
  password: "your_sql_user_password"

logging:
  level: "INFO"
  file: "./logs/app.log"

env:
  name: "dev"

sources:
  ms_kb_url: "https://sqlserverbuilds.blogspot.com/" # site used for SQL KB Catalog
